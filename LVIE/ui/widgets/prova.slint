import { Filter } from "filter.slint";
export component Example inherits Window {
    width: 200px;
    height: 100px;
    Filter {
        name: "Prova";
        minimum: -5.0;
        maximum: 5.0;
        decimals: 3;
        percent: true;
    }
}